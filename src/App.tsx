import React from 'react';

const App = () => {
  console.log('process.env.NODE_ENV', process.env.NODE_ENV);

  return (
    <>
      <div>
        테스트
      </div>
    </>
  );
};

export default App;